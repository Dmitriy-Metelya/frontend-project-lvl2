name: Linter

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install package
        env:
          DEFAULT_BRANCH: main
        run: make install
      - name: Run linter
        env:
          DEFAULT_BRANCH: main
        run: make lint
